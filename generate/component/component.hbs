import React, { FunctionComponent } from 'react'
import { Styled{{pascalCase name}}, I{{pascalCase name}}Styles } from './{{pascalCase name}}.styles'

interface I{{pascalCase name}}Props extends I{{pascalCase name}}Styles {
	/** Example {{pascalCase name}} prop please replace or remove! */
	example{{pascalCase name}}Prop?: boolean
}

export const {{pascalCase name}}:FunctionComponent<I{{pascalCase name}}Props> = (props) => (
	<Styled{{pascalCase name}} {...props}>{{pascalCase name}}</Styled{{pascalCase name}}>
)
