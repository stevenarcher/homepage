import { shallow } from 'enzyme'
import { usage } from './{{pascalCase name}}.stories'

describe('<{{pascalCase name}}>', () => {
	it('renders correctly', () => {
		const wrapper = shallow(usage())
		expect(wrapper).toMatchSnapshot()
  })
});
